name = "monet-chat-api"
main = "workers.js"
compatibility_date = "2025-11-15"

# KV 命名空间配置 - 无需提供ID，部署时会自动创建
[[kv_namespaces]]
binding = "USERS_KV"

[[kv_namespaces]]
binding = "MESSAGES_KV"

[[kv_namespaces]]
binding = "CONFIG_KV"

# 定时触发器配置 - 每小时检查一次
[[triggers]]
crons = ["0 * * * *"]  # 每小时执行一次